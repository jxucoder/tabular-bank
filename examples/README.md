# Examples

- `scripts/` contains runnable example Python entry points.
- `problems/` contains generated Kaggle-style example problem bundles.

Generate or refresh the competition-style bundles with:

```bash
python examples/scripts/export_kaggle_style_problems.py
```
